<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>testFile.log</file>
        <append>true</append>
        <encoder>
            <!-- ladi se jak ta zprava bude vypadat -->
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>


    <root level="WARN">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="FILE"/>
    </root>

    <loggerloger name="org.springframework" level="ERROR"/>
    <loggerloger name="ppj" level="INFO"/>

    <springProfile name="devel">
        <logger name="ppj" level="DEBUG"></logger>
    </springProfile>
</configuration>



        <?xml version="1.0" encoding="UTF-8"?>
<configuration>
<appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <!-- Log message format -->
    <encoder>
        <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
</appender>

<!-- Setting the logging level to WARN for code in net.javabeat.service -->
<logger name="net.javabeat.service" level="WARN" />

<!-- Setting the logging level to INFO for code in net.javabeat.model -->
<logger name="net.javabeat.model" level="INFO" />

<!-- Setting the root level of logging to INFO -->
<root level="info">
    <appender-ref ref="STDOUT" />
</root>
</configuration>


